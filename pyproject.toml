[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">=3.12"
name = "mcp-remote"
version = "0.1.0"
dependencies = [
    "fastmcp>=2.2.6",
    "pytz>=2025.2",
] 
